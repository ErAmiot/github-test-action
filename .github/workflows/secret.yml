name: Secret
on: workflow_dispatch

jobs:
    deploy:
        runs-on: ubuntu-latest
        environment: test
        env:
            API_KEY: ${{secrets.API_KEY}}
        steps:
            - name: debug
              if: ${{API_KEY}}
              run: echo "la clé n'est pas vide"
            - run: echo "La clé est ${{API_KEY}}"
