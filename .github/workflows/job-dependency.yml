name: Avec Dependance
on: workflow_dispatch

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: job1
              run: echo "Compilation"
    test:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - name: rien
              run: echo "Testing"
    deploy:
        runs-on: ubuntu-latest
        needs: [build,test]
        steps:
            - name: deploiement
              run: echo "Deploying"