name: Avec des actions
on: workflow_dispatch

jobs:
    inspect-files:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: lister les fichiers
              run: ls -l
            - name: afficher le README
              run: cat README.md